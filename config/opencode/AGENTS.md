# Global Rules

## Build and execution
Never build, compile, or run any code unless the user explicitly asks.
This includes: make, cmake, cargo, go build, npm run, pytest, or any  
other build/test/execution command. Only read and edit source files.  
